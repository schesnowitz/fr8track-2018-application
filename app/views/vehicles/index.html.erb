<p id="notice"><%= notice %></p>

<h1>Vehicles</h1>
<%= form_tag vehicles_path, method: :get do %> 
<%= text_field_tag :search_data, params[:search_data] %>
<%= submit_tag 'Search', class: 'btn btn-outline-info', name: nil %> 
<% end %>

<table class='table-striped table-bordered' id='vehicle-index-table'>
  <thead>
    <tr>
      <th>Api vehicle</th>
      <th>Number</th>
      <th>Status</th>
      <th>Ifta</th>
      <th>Vin</th>
      <th>Make</th>
      <th>Model</th>
      <th>Year</th>
      <th>Plate State</th>
      <th>Plate Number</th>
      <th>Metric?</th>

      <th>Api driver</th>
      <th>Api first name</th>
      <th>Api last name</th>
      <th>Api username</th>
      <th>Api email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @vehicles.each do |vehicle| %>
      <tr>
        <td><%= vehicle.api_vehicle_id %></td>
        <td><%= vehicle.number %></td>
        <td><%= vehicle.status %></td>
        <td><%= vehicle.ifta %></td>
        <td><%= vehicle.vin %></td>
        <td><%= vehicle.make %></td>
        <td><%= vehicle.model %></td>
        <td><%= vehicle.year %></td>
        <td><%= vehicle.license_plate_state %></td>
        <td><%= vehicle.license_plate_number %></td>
        <td><%= vehicle.metric_units %></td>

        <td><%= vehicle.api_driver_id %></td>
        <td><%= vehicle.api_first_name %></td>
        <td><%= vehicle.api_last_name %></td>
        <td><%= vehicle.api_username %></td>
        <td><%= vehicle.api_email %></td>
        <td><%= link_to 'Show', vehicle %></td>
        <td><%= link_to 'Edit', edit_vehicle_path(vehicle) %></td>
        <td><%= link_to 'Destroy', vehicle, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Vehicle', new_vehicle_path %><br>

